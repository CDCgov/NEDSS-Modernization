nbs:
  security:
    oidc:
      client:
        id: nbs-development
        secret: ${OIDC_CLIENT_SECRET}
    log:
      level: INFO
  gateway:
    log:
      level: INFO
    pagebuilder:
      enabled: true
      page:
        library:
          enabled: true
        management:
          create:
            enabled: true
          edit:
            enabled: true
