spring:
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: ${nbs.security.oidc.issuer-uri:http://localhost:8100/realms/nbs-users}
          jwk-set-uri: ${nbs.security.oidc.jwk-set-uri:http://localhost:8100/realms/nbs-users/protocol/openid-connect/certs}
