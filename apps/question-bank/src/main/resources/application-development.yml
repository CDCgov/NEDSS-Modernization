nbs:
  wildfly:
    host: localhost

logging:
  level:
    org:
      hibernate:
        SQL: DEBUG    # Logs SQL Statements generated by JPA
        type:
          descriptor:
            sql:
              BasicBinder: DEBUG  # Logs SQL Parameters of statements logged by JPA

      springframework:
        jdbc:
          core: DEBUG   # Logs SQL statements executed through the JdbcTemplate
    nbs:
      classic:
        outgoing: DEBUG   # Logs any RestTemplate interactions with NBC Classic
