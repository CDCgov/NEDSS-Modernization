import { render } from '@testing-library/react';

describe('Summary', () => {
    beforeEach(() => {
        jest.clearAllMocks();
    });

    it('should render the Summary component', async () => {});
});
