import { Term } from 'apps/search/useSearchResultDisplay';
import { LabReportFilterEntry } from './labReportFormTypes';

const labReportTermsResolver = (entry: LabReportFilterEntry): Term[] => {
    const terms: Term[] = [];

    return terms;
};

export { labReportTermsResolver };
