services:
  api:
    build:
      context: ./
      dockerfile: ./api/Dockerfile
    container_name: api
    ports:
      - 8080:8080
    networks:
      - nbs
networks:
  nbs:
    name: nbs
    driver: bridge